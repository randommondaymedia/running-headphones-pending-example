# running headphones

Generated by @affiliate-os/site-factory.

## Local dev

```bash
pnpm install
pnpm dev
```

The site renders from `src/data/pages.json` via Astro's `getStaticPaths()`.
Update the JSON to add or remove pages — no Markdown files needed.

## Affiliate disclosure

Every page includes the disclosure component. Edit
`src/components/AffiliateDisclosure.astro` to customize.
